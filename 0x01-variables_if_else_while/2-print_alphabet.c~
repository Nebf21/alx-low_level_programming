#include <stdio.h>
0;276;0c/**
 * prints in lower case
 *
 *
 */

int main()
{
  int alpha;
  char c;

 for(alpha=97; alpha=<122; alpha++)
    {
      c=alpha;
      putchar(c);
	}
 putchar('\n');
  return (0);
}


